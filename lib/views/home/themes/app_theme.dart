import 'package:flutter/material.dart';

class AppTheme {
  // Colors
  static const Color cardBackground = Color.fromRGBO(45, 32, 84, 1);
  static const Color background = Color(0xFFF8F8F8);
  static const Color titleColor = Colors.white;
  static const Color subtitleColor = Colors.white70;

  // Sizes
  static const double cardMargin = 8.0;
  static const double borderRadius = 12.0;
  static const double cardTitleFontSize = 17.0;
  static const double cardValueFontSize = 25.0;
  static const double cardSubtitleFontSize = 14.0;

  // Paddings
  static const EdgeInsets cardPadding = EdgeInsets.all(16);
  static const EdgeInsets screenPadding = EdgeInsets.all(16);
}